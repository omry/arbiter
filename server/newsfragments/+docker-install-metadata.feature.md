Docker install now records the installed target path in the staging directory,
writes non-secret install metadata into the installed deployment directory,
prints the installed MCP URL, and smoke-tests the service after startup.
