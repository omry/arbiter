# Arbiter Website

This directory contains the Docusaurus website.

```bash
npm install
npm run start
npm run build
```

Terminal recordings live under `static/casts/`. Rendered images for docs live
under `static/img/casts/`.
