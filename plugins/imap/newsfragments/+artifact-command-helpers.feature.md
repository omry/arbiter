Recommend transient artifact command helpers for binary IMAP attachments instead
of persistent local saves, while documenting `arbiter artifact save` as available
only when explicitly requested by the user.
