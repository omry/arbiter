Added attachment metadata to `imap:get_message` results and one-time
artifact URL delivery for `imap:get_attachment` so agents can identify and
open attached documents without returning attachment bytes in tool results.
