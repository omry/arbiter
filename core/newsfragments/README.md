# News Fragments

Use towncrier fragments here for user-visible changes to `arbiter-core`.

Fragment filenames use `<id>.<type>.md`. Use a GitHub issue or PR number as the
id when one exists, or the `+` orphan prefix when one does not.
